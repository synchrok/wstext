TASK 5: Settings Store Creation
================================

FILE CREATED: src/lib/stores/settings.svelte.ts

EXPORTS:
✓ appSettings ($state<AppSettings>) — reactive settings object
✓ loadSettings() — async load from Tauri store
✓ saveSettings() — async persist to Tauri store
✓ updateSetting<K>() — update single setting with debounced save
✓ updateTheme() — convenience function for theme changes
✓ updateFontSize() — convenience function for font size (clamped 8-40)

IMPLEMENTATION DETAILS:
- Uses LazyStore from @tauri-apps/plugin-store (loads on first access)
- Svelte 5 $state rune for reactivity
- 300ms debounce on saves to avoid excessive disk writes
- Merges saved settings with defaults for forward compatibility
- Non-blocking error handling with console.warn fallback

VERIFICATION:
$ npm run check
> svelte-check found 0 errors and 0 warnings ✓

STATUS: COMPLETE
